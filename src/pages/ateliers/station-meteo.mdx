---
title: 'Station météo'
description: "Mesure la température avec un capteur DS18B20 (1-Wire) et affiche les données sur un écran OLED SSD1306."
level: 'debutant'
duration: '3h'
tags: ['ESP32', 'DS18B20', 'OLED', 'I2C', '1-Wire']
---

import Layout from '../../layouts/Layout.astro';
import GitHubCodeBlock from '../../components/GitHubCodeBlock.astro';
import DifficultyBadge from '../../components/DifficultyBadge.astro';
import { siteUrl } from '../../utils/url';

<Layout title="Atelier 03 — Station météo" description="Mesure la température avec un capteur DS18B20 et affiche les données sur un écran OLED SSD1306.">

<div class="border-b border-base-200/60 bg-base-200/20">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-3">
    <div class="flex items-center gap-2 text-sm text-base-content/50">
      <a href={siteUrl('/')} class="hover:text-base-content transition-colors">Accueil</a>
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/></svg>
      <a href={siteUrl('/ateliers/')} class="hover:text-base-content transition-colors">Ateliers</a>
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/></svg>
      <span class="text-base-content/80">Station météo</span>
    </div>
  </div>
</div>

<section class="py-12 hero-pattern border-b border-base-200/60">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center gap-3 mb-6">
      <span class="font-mono text-primary/60 text-sm font-bold bg-primary/10 px-3 py-1 rounded-lg">ATELIER 03</span>
      <DifficultyBadge level="debutant" />
    </div>
    <h1 class="text-3xl sm:text-4xl font-black mb-4">Station météo</h1>
    <p class="text-base-content/65 text-lg leading-relaxed max-w-2xl mb-8">
      Mesure la température avec un capteur DS18B20 (1-Wire) et affiche les données sur un écran OLED SSD1306. Un seul fil suffit pour lire le capteur !
    </p>
    <div class="flex flex-wrap gap-4">
      <div class="flex items-center gap-2 text-sm text-base-content/60">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
        3h
      </div>
      <div class="flex flex-wrap gap-2">
        {['ESP32', 'DS18B20', 'OLED', 'I2C', '1-Wire'].map(tag => (
          <span class="text-xs px-2.5 py-1 rounded-lg bg-base-200/60 text-base-content/50 font-mono">{tag}</span>
        ))}
      </div>
    </div>
  </div>
</section>

<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12 mdx-content">

## Ce que tu vas créer

Une station météo qui lit la température en temps réel et l'affiche sur un petit écran OLED. Le projet combine deux protocoles de communication différents :
- Le protocole **1-Wire** pour lire le capteur DS18B20 (un seul fil de données)
- Le protocole **I2C** pour piloter l'écran OLED (deux fils : SDA et SCL)

---

## Matériel nécessaire

<div class="overflow-x-auto rounded-xl border border-base-200/80 my-6">
  <table class="table table-zebra w-full">
    <thead>
      <tr><th>Composant</th><th>Quantité</th><th>Notes</th></tr>
    </thead>
    <tbody>
      <tr><td class="font-medium">ESP32</td><td>1</td><td class="text-base-content/60">Fourni en atelier</td></tr>
      <tr><td class="font-medium">Capteur DS18B20</td><td>1</td><td class="text-base-content/60">Version TO-92 ou module étanche</td></tr>
      <tr><td class="font-medium">Résistance 4.7 kΩ</td><td>1</td><td class="text-base-content/60">Pull-up obligatoire sur le fil DATA</td></tr>
      <tr><td class="font-medium">Écran OLED SSD1306 128x64</td><td>1</td><td class="text-base-content/60">Interface I2C (4 broches)</td></tr>
      <tr><td class="font-medium">Câble micro-USB</td><td>1</td><td class="text-base-content/60">Fourni en atelier</td></tr>
    </tbody>
  </table>
</div>

## Connexions

<div class="rounded-xl overflow-hidden border border-base-200/60 bg-[#1a1b26] my-4">
  <div class="px-4 py-2 border-b border-white/8 text-white/50 text-xs font-mono">câblage</div>
  <pre class="p-4 text-xs font-mono text-white/70 leading-relaxed"><code>{`DS18B20 VCC  ──── ESP32 3.3V
DS18B20 GND  ──── ESP32 GND
DS18B20 DATA ──── ESP32 GPIO27  (+ résistance 4.7kΩ vers 3.3V)

OLED VCC  ──── ESP32 3.3V
OLED GND  ──── ESP32 GND
OLED SDA  ──── ESP32 GPIO21
OLED SCL  ──── ESP32 GPIO22`}</code></pre>
</div>

---

## Le code

<GitHubCodeBlock
  repo="vincent-herm/fablab-esp32-ateliers"
  file="atelier3-station-meteo/main.py"
  language="python"
  title="Station météo MicroPython — main.py"
/>

---

## Comment ça marche

### Lire le capteur DS18B20

Le protocole 1-Wire permet de connecter plusieurs capteurs sur un seul fil. MicroPython intègre une librairie dédiée :

```python
import onewire, ds18x20
from machine import Pin

# Initialiser le bus 1-Wire sur GPIO27
ow = onewire.OneWire(Pin(27))
ds = ds18x20.DS18X20(ow)

# Chercher les capteurs connectés
roms = ds.scan()

# Lancer une mesure puis lire
ds.convert_temp()
time.sleep_ms(750)          # attendre la conversion (750ms max)
temp = ds.read_temp(roms[0])
print(f"Température : {temp:.1f} °C")
```

### Afficher sur l'écran OLED

```python
from machine import I2C
import ssd1306

i2c = I2C(0, scl=Pin(22), sda=Pin(21))
oled = ssd1306.SSD1306_I2C(128, 64, i2c)

oled.fill(0)                         # effacer l'écran
oled.text("Station meteo", 0, 0)
oled.text(f"Temp: {temp:.1f} C", 0, 20)
oled.show()                          # envoyer vers l'écran
```

---

## Pour aller plus loin

<div class="grid grid-cols-1 sm:grid-cols-3 gap-4 my-6">
  {[
    { title: 'Ajouter humidité DHT22', desc: 'Branche un capteur DHT22 pour mesurer aussi le taux d\'humidité et l\'afficher sur l\'OLED.' },
    { title: 'Alertes WiFi', desc: 'Envoie une notification sur ton téléphone quand la température dépasse un seuil.' },
    { title: 'Plusieurs capteurs sur un fil', desc: 'Le protocole 1-Wire supporte plusieurs DS18B20 sur le même fil — mesure différentes pièces.' },
  ].map(card => (
    <div class="rounded-xl border border-base-200/80 bg-base-200/30 p-5">
      <h4 class="font-semibold text-sm mb-2">{card.title}</h4>
      <p class="text-xs text-base-content/60">{card.desc}</p>
    </div>
  ))}
</div>

<div class="border-t border-base-200/60 pt-10 mt-10 flex items-center justify-between">
  <a href={siteUrl('/ateliers/neopixel/')} class="group flex items-center gap-3 rounded-xl border border-base-200/80 bg-base-200/30 px-5 py-4 hover:border-primary/30 transition-colors">
    <svg class="w-5 h-5 text-base-content/40 group-hover:text-primary group-hover:-translate-x-1 transition-all" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/></svg>
    <div><div class="text-xs text-base-content/50 mb-0.5">Atelier précédent</div><div class="font-semibold text-sm group-hover:text-primary transition-colors">LED RGB et NeoPixel</div></div>
  </a>
  <a href={siteUrl('/ateliers/lumiere-auto/')} class="group flex items-center gap-3 rounded-xl border border-base-200/80 bg-base-200/30 px-5 py-4 hover:border-primary/30 transition-colors">
    <div class="text-right"><div class="text-xs text-base-content/50 mb-0.5">Atelier suivant</div><div class="font-semibold text-sm group-hover:text-primary transition-colors">Lumière automatique</div></div>
    <svg class="w-5 h-5 text-base-content/40 group-hover:text-primary group-hover:translate-x-1 transition-all" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/></svg>
  </a>
</div>

</div>
</Layout>
