---
title: 'LED RGB et NeoPixel'
description: "Anime un bandeau de LED RGB WS2812 avec MicroPython : couleurs, défilement, arc-en-ciel et effets aléatoires."
level: 'debutant'
duration: '3h'
tags: ['ESP32', 'NeoPixel', 'WS2812', 'RGB', 'Animations']
---

import Layout from '../../layouts/Layout.astro';
import GitHubCodeBlock from '../../components/GitHubCodeBlock.astro';
import ToiDeJouer from '../../components/ToiDeJouer.astro';
import Prediction from '../../components/Prediction.astro';
import DifficultyBadge from '../../components/DifficultyBadge.astro';
import { siteUrl } from '../../utils/url';

<Layout title="Atelier 02 — LED RGB et NeoPixel" description="Anime un bandeau de LED RGB WS2812 avec MicroPython.">

<div class="border-b border-base-200/60 bg-base-200/20">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-3">
    <div class="flex items-center gap-2 text-sm text-base-content/50">
      <a href={siteUrl('/')} class="hover:text-base-content transition-colors">Accueil</a>
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/></svg>
      <a href={siteUrl('/ateliers/')} class="hover:text-base-content transition-colors">Ateliers</a>
      <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/></svg>
      <span class="text-base-content/80">LED RGB et NeoPixel</span>
    </div>
  </div>
</div>

<section class="py-12 hero-pattern border-b border-base-200/60">
  <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
    <div class="flex items-center gap-3 mb-6">
      <span class="font-mono text-primary/60 text-sm font-bold bg-primary/10 px-3 py-1 rounded-lg">ATELIER 02</span>
      <DifficultyBadge level="debutant" />
    </div>
    <h1 class="text-3xl sm:text-4xl font-black mb-4">LED RGB et NeoPixel</h1>
    <p class="text-base-content/65 text-lg leading-relaxed max-w-2xl mb-8">
      Prends le contrôle d'un bandeau de 16 LED RGB WS2812. Couleurs sur mesure, animations de défilement, arc-en-ciel automatique — tout ça avec un seul fil de données.
    </p>
    <div class="flex flex-wrap gap-4">
      <div class="flex items-center gap-2 text-sm text-base-content/60">
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
        3h
      </div>
      <div class="flex flex-wrap gap-2">
        {['ESP32', 'NeoPixel', 'WS2812', 'RGB', 'Animations'].map(tag => (
          <span class="text-xs px-2.5 py-1 rounded-lg bg-base-200/60 text-base-content/50 font-mono">{tag}</span>
        ))}
      </div>
    </div>
  </div>
</section>

<div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8 py-12 mdx-content">

## Ce que tu vas créer

Un bandeau NeoPixel qui propose 6 animations différentes. Le bouton BOOT de l'ESP32 fait défiler les modes :
- Défilement bleu / rouge
- Arc-en-ciel automatique
- Scintillement aléatoire
- Remplissage progressif

---

## Matériel nécessaire

<div class="overflow-x-auto rounded-xl border border-base-200/80 my-6">
  <table class="table table-zebra w-full">
    <thead>
      <tr><th>Composant</th><th>Quantité</th><th>Notes</th></tr>
    </thead>
    <tbody>
      <tr><td class="font-medium">ESP32</td><td>1</td><td class="text-base-content/60">Fourni en atelier</td></tr>
      <tr><td class="font-medium">Bandeau NeoPixel WS2812</td><td>1 (16 LEDs)</td><td class="text-base-content/60">Fourni en atelier</td></tr>
      <tr><td class="font-medium">Condensateur 1000 µF</td><td>1</td><td class="text-base-content/60">Protection contre les pics de courant</td></tr>
      <tr><td class="font-medium">Résistance 300-500 Ω</td><td>1</td><td class="text-base-content/60">Sur le fil DATA</td></tr>
      <tr><td class="font-medium">Câble micro-USB</td><td>1</td><td class="text-base-content/60">Fourni en atelier</td></tr>
    </tbody>
  </table>
</div>

## Connexions

<div class="rounded-xl overflow-hidden border border-base-200/60 bg-[#1a1b26] my-4">
  <div class="px-4 py-2 border-b border-white/8 text-white/50 text-xs font-mono">câblage</div>
  <pre class="p-4 text-xs font-mono text-white/70 leading-relaxed"><code>{`ESP32 5V   ──── NeoPixel 5V
ESP32 GND  ──── NeoPixel GND
ESP32 G18  ──── résistance 300Ω ──── NeoPixel DIN`}</code></pre>
</div>

---

## Le code

<GitHubCodeBlock
  repo="vincent-herm/fablab-esp32-ateliers"
  file="atelier2-neopixel/main.py"
  language="python"
  title="NeoPixel MicroPython — main.py"
/>

<Prediction question="Tu modifies `np[3] = (255, 0, 0)` mais tu oublies `np.write()`. Que se passe-t-il ?">

**Rien du tout.** La LED 3 reste dans son état précédent.

`np[3] = (255, 0, 0)` ne fait que noter la couleur **en mémoire**, dans l'ESP32. C'est `np.write()` qui envoie réellement toutes les couleurs vers le bandeau, sur le fil de données.

C'est d'ailleurs pratique : on prépare les 16 LEDs tranquillement, puis un seul `write()` les met toutes à jour d'un coup — donc sans scintillement.

</Prediction>

<ToiDeJouer>

- Change la couleur du défilement : `couleur=(255, 0, 0)` pour du rouge
- Change la vitesse : `delai=20` au lieu de `80`
- Baisse l'intensité : `(0, 20, 60)` au lieu de `(0, 80, 255)` — bien moins éblouissant, et ça consomme trois fois moins
- Si ton bandeau n'a pas 16 LEDs, ajuste `N = 16`
- Dans l'arc-en-ciel, remplace `i * 16` par `i * 4` → les couleurs se resserrent
- Plus dur : fais un chenillard qui **rebondit** (aller-retour) au lieu de repartir à gauche

</ToiDeJouer>


---

## Comment ça marche

### Couleurs RGB

Chaque LED NeoPixel est contrôlée par un triplet `(rouge, vert, bleu)`, chaque valeur de 0 à 255 :

```python
np[0] = (255, 0, 0)    # rouge pur
np[1] = (0, 255, 0)    # vert pur
np[2] = (0, 0, 255)    # bleu pur
np[3] = (255, 165, 0)  # orange
np[4] = (128, 0, 128)  # violet
np.write()             # envoyer vers les LEDs
```

### Un seul fil pour 16 LEDs

C'est la particularité des WS2812 : **un seul fil de données** commande tout le bandeau, quelle que soit sa longueur.

Chaque LED contient un minuscule circuit qui lit les 24 premiers bits qui lui parviennent (8 pour le rouge, 8 pour le vert, 8 pour le bleu), les garde pour elle, et **retransmet tout le reste** à sa voisine. De proche en proche, chacune se sert au passage.

D'où deux conséquences pratiques :

- **L'ordre compte.** `np[0]` est toujours la LED la plus proche de l'ESP32.
- **Rien ne change tant qu'on n'a pas appelé `np.write()`.** Modifier `np[3]` ne fait que préparer la couleur en mémoire ; c'est `write()` qui envoie la trame complète sur le fil.

### La fonction arc-en-ciel

`roue(pos)` transforme un nombre de 0 à 255 en une couleur, en parcourant le cercle des teintes. Elle travaille par tiers :

| `pos` | Transition |
|---|---|
| 0 → 84 | rouge qui s'efface, vert qui monte |
| 85 → 169 | vert qui s'efface, bleu qui monte |
| 170 → 255 | bleu qui s'efface, rouge qui remonte |

À chaque instant, la somme des trois composantes reste constante — la luminosité ne varie pas, seule la teinte tourne. Et comme `pos = pos % 256` ramène toujours dans l'intervalle, on peut incrémenter indéfiniment : l'arc-en-ciel boucle tout seul.

Le décalage `i * 16` dans l'animation donne à chaque LED une teinte légèrement différente de sa voisine (16 LEDs × 16 = 256, soit un tour complet des teintes) : c'est ce qui étale l'arc-en-ciel sur le bandeau au lieu de le faire clignoter d'un bloc.

### Attention à la consommation

Une LED WS2812 à fond en blanc (`255, 255, 255`) tire environ **60 mA**. Pour 16 LEDs, cela fait presque 1 A — bien plus que ce qu'un port USB fournit confortablement.

En atelier, on reste donc sur des couleurs modérées. Pour un bandeau plus long ou à pleine puissance, il faut une **alimentation 5 V séparée**, avec la masse reliée à celle de l'ESP32.

---

## Pour aller plus loin

<div class="grid grid-cols-1 sm:grid-cols-3 gap-4 my-6">
  {[
    { title: 'Contrôle par boutons', desc: 'Utilise 3 boutons pour choisir la couleur rouge/vert/bleu en temps réel.' },
    { title: 'Réagir au son', desc: 'Branche un micro et fais pulser les LEDs selon le volume ambiant.' },
    { title: 'Bandeau plus long', desc: 'Adapte le code pour 30, 60 ou 144 LEDs — change juste N.' },
  ].map(card => (
    <div class="rounded-xl border border-base-200/80 bg-base-200/30 p-5">
      <h4 class="font-semibold text-sm mb-2">{card.title}</h4>
      <p class="text-xs text-base-content/60">{card.desc}</p>
    </div>
  ))}
</div>

---

## Six défis de plus — avec un seul bouton en plus

Pour ces défis, ajoute **un bouton poussoir** : une patte sur G5, l'autre sur GND (la résistance interne de l'ESP32 est activée par le programme, aucun autre composant). Le bandeau reste sur **G18**. Pour chaque défi, copie le fichier dans Thonny, enregistre-le sur l'ESP32 sous le nom `main.py`, et lance-le.

### 1. La LED qui avance à chaque appui

Un appui sur le bouton = un cran. Pas d'animation automatique : c'est toi qui tiens le rythme.

<GitHubCodeBlock
  repo="vincent-herm/fablab-esp32-ateliers"
  file="atelier2-neopixel/aller-plus-loin/01_pas_a_pas.py"
  language="python"
  title="01_pas_a_pas.py"
/>

<ToiDeJouer>

- Fais **reculer** la LED au lieu de la faire avancer
- Fais avancer la LED de **deux** crans à chaque appui
- Ajoute un second sens : un appui court avance, un appui long recule

</ToiDeJouer>

### 2. Un compteur binaire

Chaque LED est un bit : LED 0 = 1, LED 1 = 2, LED 2 = 4, et ainsi de suite jusqu'à 128 avec la LED 7 (le programme compte sur 8 bits, les LEDs 8 à 15 restent éteintes). C'est exactement comme ça qu'un ordinateur compte.

<GitHubCodeBlock
  repo="vincent-herm/fablab-esp32-ateliers"
  file="atelier2-neopixel/aller-plus-loin/02_compteur_binaire.py"
  language="python"
  title="02_compteur_binaire.py"
/>

<Prediction question="Tu appuies 5 fois sur le bouton. Quelles LEDs sont allumées ?">

**Les LEDs 0 et 2.**

5 = 4 + 1, soit `00000101` en binaire : la LED 0 vaut 1 et la LED 2 vaut 4. Regarde aussi le Shell de Thonny : le programme y écrit le nombre en décimal **et** en binaire.

Et à 255 ? Les 8 premières LEDs sont toutes allumées. Le 256ᵉ appui remet tout à zéro : avec 8 bits, on ne peut pas aller plus loin.

</Prediction>

<ToiDeJouer>

- Ton bandeau a 16 LEDs : mets `BITS = 16` et compte jusqu'à **65 535**. Combien d'appuis pour allumer la LED 15 ? (Indice : 32 768.)
- Change la couleur des LEDs allumées : `(0, 20, 60)`

</ToiDeJouer>

### 3. Le jeu de réflexes

Un point bleu fait des allers-retours. Appuie sur le bouton quand il passe sur la LED verte du milieu. Chaque victoire l'accélère, une erreur remet tout à zéro.

<GitHubCodeBlock
  repo="vincent-herm/fablab-esp32-ateliers"
  file="atelier2-neopixel/aller-plus-loin/03_jeu_reflexes.py"
  language="python"
  title="03_jeu_reflexes.py"
/>

<ToiDeJouer>

- Change la cible : `CIBLE = 0` pour viser le bout du bandeau
- Rends le jeu plus dur : `delai - 25` au lieu de `delai - 15`
- Ajoute un défi à deux : le premier qui rate perd, l'autre gagne le point

</ToiDeJouer>

### 4. Un dé électronique

Le dé tire des valeurs au hasard de plus en plus lentement, puis s'arrête. Le résultat est le nombre de LEDs allumées, de 1 à 6.

<GitHubCodeBlock
  repo="vincent-herm/fablab-esp32-ateliers"
  file="atelier2-neopixel/aller-plus-loin/04_de_electronique.py"
  language="python"
  title="04_de_electronique.py"
/>

<ToiDeJouer>

- Passe à un dé à 12 faces : `random.randint(1, 12)`
- Change la vitesse du ralentissement : `delai * 1.25` devient `delai * 1.1`
- Lance le dé 60 fois : comptes-tu vraiment autant de 6 que de 1 ?

</ToiDeJouer>

### 5. Une veilleuse qui respire

Tout le bandeau monte et descend en luminosité. Un appui court change la couleur, un appui long change l'intensité maximale.

<GitHubCodeBlock
  repo="vincent-herm/fablab-esp32-ateliers"
  file="atelier2-neopixel/aller-plus-loin/05_veilleuse.py"
  language="python"
  title="05_veilleuse.py"
/>

<ToiDeJouer>

- Ajoute une couleur à la liste `COULEURS`
- Ralentis la respiration : remplace `time.sleep_ms(20)` par `time.sleep_ms(40)`
- Le programme distingue un appui court d'un appui long : trouve où, et change la limite de 600 ms

</ToiDeJouer>

### 6. Le chenillard qui rebondit

C'est le défi proposé plus haut, avec une traînée qui s'éteint derrière le point rouge. Un appui sur le bouton change la vitesse.

<GitHubCodeBlock
  repo="vincent-herm/fablab-esp32-ateliers"
  file="atelier2-neopixel/aller-plus-loin/06_chenillard_rebond.py"
  language="python"
  title="06_chenillard_rebond.py"
/>

<ToiDeJouer>

- Allonge la traînée : remplace `* 2 // 3` par `* 4 // 5`
- Change la couleur de la tête : `(255, 0, 0)` devient `(0, 100, 255)`
- Fais varier la couleur de la tête à chaque rebond

</ToiDeJouer>

<div class="border-t border-base-200/60 pt-10 mt-10 flex items-center justify-between">
  <a href={siteUrl('/ateliers/initiation/')} class="group flex items-center gap-3 rounded-xl border border-base-200/80 bg-base-200/30 px-5 py-4 hover:border-primary/30 transition-colors">
    <svg class="w-5 h-5 text-base-content/40 group-hover:text-primary group-hover:-translate-x-1 transition-all" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M15 19l-7-7 7-7"/></svg>
    <div><div class="text-xs text-base-content/50 mb-0.5">Atelier précédent</div><div class="font-semibold text-sm group-hover:text-primary transition-colors">Initiation — Zéro câblage</div></div>
  </a>
  <a href={siteUrl('/ateliers/station-meteo/')} class="group flex items-center gap-3 rounded-xl border border-base-200/80 bg-base-200/30 px-5 py-4 hover:border-primary/30 transition-colors">
    <div class="text-right"><div class="text-xs text-base-content/50 mb-0.5">Atelier suivant</div><div class="font-semibold text-sm group-hover:text-primary transition-colors">Station météo</div></div>
    <svg class="w-5 h-5 text-base-content/40 group-hover:text-primary group-hover:translate-x-1 transition-all" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7"/></svg>
  </a>
</div>

</div>
</Layout>
